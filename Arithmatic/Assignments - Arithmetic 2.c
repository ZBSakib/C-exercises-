#include <stdio.h>

int main() {
    int u;
    printf("Enter a positive integer number:");
    scanf("%d", &u)

        for (int i = -5; i <= u; i++) {
            printf("%d ", i);
        }
    }

}

/*Write a C program that computes square root and square of a given non negative real number. 
Print results at separate rows.*/